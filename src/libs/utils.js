export function studentIdToKey(id) {
    return `student_${id}`
}

export function courseIdToKey(id) {
    return `course_${id}`
}

export function activityIdToKey(id) {
    return `activity_${id}`
}
